using System;

namespace Arrayer
{
    class Program
    {

        static void Main()
        {

            int[] tal = new int[5];
            Console.WriteLine("Skriv 5 tal");
            for (int i = 0; i < 5; i++)
            {
                tal[i] = int.Parse(Console.ReadLine());
            }

            for (int i = 4; i >= 0; i--)
            {
                Console.WriteLine(tal[i]);
            }
        }
    }
}
